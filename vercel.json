{
  "build": {
    "env": {
      "GO111MODULE": "on"
    },
    "command": "go build ./cmd/server && mv server /vercel/path0/server"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/vercel/path0/$1",
      "methods": ["GET", "HEAD", "PUT", "PATCH", "POST", "DELETE"]
    }
  ]
}
